# DESIGN.md — Codex Harness Desktop 视觉规范

> **这份文件给谁读**：任何要改这个仓库 UI 的 agent（和人）。
>
> | 文件 | 谁读 | 定义什么 |
> |---|---|---|
> | `AGENTS.md` | 编码 agent | 项目**怎么建**（架构分层、IPC 契约、守卫口径） |
> | `DESIGN.md` | 设计/改 UI 的 agent | 项目**长什么样**（色板、字体、间距、组件约定） |
>
> 概念来自 Google Stitch 的 [DESIGN.md](https://stitch.withgoogle.com/docs/design-md/overview/)；
> 结构参考 [VoltAgent/awesome-design-md](https://github.com/VoltAgent/awesome-design-md)（MIT）。
> 本文件里**所有数值都是从 `src/styles/*.css` 实测取出的**，不是凭印象写的。
>
> ⛔ **改了 CSS 变量就必须同步这里**——否则这份文件会变成骗人的文档（比没有还糟）。

---

## Overview

**设计语言**：克制的暖灰中性色 + 单一蓝色主色 + 语义色点缀。低饱和、轻描边、少阴影。

- 中性色偏**暖**（`#23231f` 墨里带黄、`#f8f8f7` 面板是暖白），不是纯灰 —— 这是全站基调，换成冷灰会立刻"不像本产品"。
- 主色只有**一个蓝**（`--accent`）。品牌识别靠它，不要引入第二种主色。
- 语义色只在**有含义**时用（成功/失败/警告/增删），不做装饰。
- 深色主题不是"把亮色反过来"，而是**另一套调过的值**（见下表）—— 反色会发灰发脏。

---

## Colors

### 变量表（亮 / 暗）

定义在 `src/styles/01-base-and-chrome.css` 的 `:root` 与 `:root[data-theme="dark"]`。

| 变量 | 亮色 | 暗色 | 用途 |
|---|---|---|---|
| `--bg` | `#fff` | `#1b1b1a` | 应用底色（最底一层） |
| `--panel` | `#f8f8f7` | `#222221` | 卡片 / 面板 / 弹层 |
| `--panel-2` | `#f1f1ef` | `#2c2c2b` | 面板内的次级块（hover、内嵌区） |
| `--line` | `#e7e7e4` | `#373736` | 边框、分隔线 |
| `--line-soft` | `#efefec` | `#2e2e2d` | 更淡的分隔（同组内） |
| `--text` | `#23231f` | `#ececea` | 正文 |
| `--muted` | `#6f6f69` | `#a6a6a1` | 次要文字、说明 |
| `--faint` | `#9b9b95` | `#7d7d78` | 占位、禁用、时间戳 |
| `--accent` | `#2f6bdd` | `#7cabf8` | 主色（按钮、链接、选中） |
| `--accent-text` | `#fff` | `#161615` | 主色**底上**的文字 |
| `--accent-soft` | `#2f6bdd14` | `#7cabf81f` | 主色淡底（标签、选中行） |
| `--link` | `#2f6bdd` | `#7cabf8` | 链接 |
| `--ok` | `#3fb36b` | `#58c085` | 成功 |
| `--red` | `#d94f43` | `#ec7a70` | 失败 / 危险 |
| `--orange` | `#e8830c` | `#f0a35c` | 警告 |
| `--purple` | `#8b5cf6` | `#a78bfa` | 特殊标记 |
| `--green` | `#1a7f37` | `#4ec27a` | 正向语义（另有 `--green-dark` 作底） |
| `--add` / `--del` | `#1a7f37` / `#d94f43` | `#6cc281` / `#ec7a70` | diff 增 / 删 |
| `--shadow` | `0 1px 2px #14140f0a, 0 10px 32px #14140f12` | `0 1px 3px #00000059, 0 10px 30px #00000059` | 唯一的阴影变量 |

### 用法规则

1. **组件里不许写死色值**。要颜色就用变量 —— 写 `#fff` / `#000` 会在暗色主题下直接瞎掉。
2. **层级靠 `--bg → --panel → --panel-2` 递进**，不要自己调明度（会破坏主题一致性）。
3. **语义色只表意**：`--ok` 只用于成功态，`--red` 只用于失败/危险。想"醒一点"请加大字号或用 `--accent`。
4. `--accent-text` 必须配 `--accent` 底用（亮色下是白字蓝底、暗色下是**深字**浅蓝底——它是专门为了对比度反转的）。

---

## Typography

- **正文字体栈**：`Inter, "Segoe UI", system-ui, sans-serif`（定义在 `01-base-and-chrome.css`）。
- **等宽字体栈**（`--mono`）：`"Cascadia Code", Consolas, ui-monospace, monospace` —— 代码、路径、命令行、token 一律用它。
- 字号没有统一的 scale 变量，**按组件约定**：正文 12–13px、次要说明 11–12px、标题 14–16px。
- `font-synthesis: none` + `text-rendering: optimizelegibility` 已在根设置 —— 不要再单独覆盖。

---

## Layout & Spacing

- 没有全局间距变量（这是现状，不是规范）。间距按**组件就近约定**写，新增时以 4px 为栅格。
- 圆角同理：各组件自定，常见区间 6–14px；弹层/大卡片偏大（14px），小控件偏小（6–8px）。
- 阴影**只用 `--shadow`** 一个变量。想更强的层次请加边框（`--line`）而不是叠阴影。

---

## Components（真实类名，照着找）

主要容器/卡片类（按出现频次）：

| 类名 | 是什么 |
|---|---|
| `.app-shell` | 应用外框（三栏布局的根） |
| `.turn-group` | 对话回合组（时间线里的一轮） |
| `.action-card` | 工具/命令执行卡片 |
| `.cmd-card` | 命令类卡片 |
| `.approval-card` | 审批请求卡片 |
| `.system-event` | 系统事件行（压缩线、通知等） |
| `.composer-menu-pop` | 输入框浮层（`#` / `@` / 命令面板） |
| `.team-cluster-body` | 专家团聚类块 |
| `.ofc-*` | 虚拟办公室插画（`src/styles/20-team-office.css`） |

**命令面板**（`.command-palette`）：两列，左列命令名（`code`，12px 等宽）、右列注释（11px）；
⛔ 两列必须**按文字基线对齐**（`align-items: baseline`）—— 按盒子居中会让两种字号的字形错开约 2px，肉眼就是"歪了"（实测过）。

---

## Theming

- 机制：根元素上的 `data-theme="dark"`（`html[data-theme="dark"]` / `:root[data-theme="dark"]`）。
- 两套值都写在 `01-base-and-chrome.css`，**改一个主题要同时核另一个**。
- ⛔ **固定插画不跟随主题**：虚拟办公室（`.office-scene`）是自带光影的浅色插画，跟主题反色会发脏 —— 它被刻意排除在主题之外。
- ⛔ 新增 CSS 变量必须在 CSS 里**给出定义**。只 `var(--x)` 引用而不定义，会被「CSS 变量对账」守卫判红（踩过：`--wk-x` / `--wk-y`）。

---

## Do's and Don'ts

### Do

- 用 `--panel` / `--line` / `--text` 表达层级，而不是手写灰色。
- 新增语义色时**同时补亮暗两套值**，并登记到本文件。
- 改 UI 后跑一次预检（`node scripts/check-preflight.mjs`）—— 里面有 CSS 变量对账与样式接入的断言。
- 纯视觉改动**先渲染出来看**（组件可以脱离宿主单独渲染 + 按真实时间截图），不要靠读代码判断"好不好看"。

### Don't

- 不要在组件里硬编码 `#fff` / `#000` / 具体灰值。
- 不要让固定插画（办公室等）跟随主题反色。
- 不要为了"更醒目"引入第二主色或高饱和渐变 —— 全站基调是低饱和。
- 不要叠多层阴影；层次用边框 + `--panel-2` 表达。
- 不要在没有截图验证的情况下声称"视觉改好了"（这个项目里已多次出现"代码看着对、渲染出来错"）。

---

## Known Gaps（诚实清单）

- **没有统一的间距/圆角/字号 scale**：各组件就近约定，跨组件一致性靠人工把关。
- **变量只有 24 个**：语义色之外的领域（如状态条、编辑器高亮）存在局部硬编码。
- **20 个 CSS 文件按数字前缀分层**（`01-base-and-chrome` … `20-team-office`），但层与层的边界没有强制约束。
- 深色主题是**另一套手调值**，不是从亮色程序化派生 —— 新增颜色时最容易漏的就是它。

---

## 维护约定

1. 改 `src/styles/01-base-and-chrome.css` 里的变量 ⇒ **同步本文件的变量表**。
2. 新增组件类名（尤其是跨文件复用的）⇒ 补进 Components 表。
3. 本文件由「CSS 变量对账」之外的**人工**维护；怀疑它与代码不一致时，以 `src/styles/*.css` 为准并回来修正这里。
