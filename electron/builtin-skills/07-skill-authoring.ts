/**
 * builtin-skills 的「skill-authoring」部分（09-22 新增，非搬迁）。
 * 元技能：教引擎**自己造技能** —— 也就是「经验包」的积累通道（用户在 09-22 点名：
 * 记忆板块太弱、坑不记；记忆层补齐后，另一半是"可复用流程"要能沉淀下来）。
 * ⛔ 正文必须是字面量（不要从 skill-pack 拼装）：预检【86】按源文件文本做安全扫描，
 *    运行时拼装会让扫描器看不见内容 ⇒ 「内置技能零 critical/high」变成恒真（记过的假绿形态）。
 * 落点常量（name / 目录 / 技能名）唯一真相源在 electron/skill-pack.ts，预检【103】断言两处同源。
 */
export const SKILL_AUTHORING_SKILL = `---
name: skill-authoring
description: 把干成的活沉淀成可复用的技能（经验包）：什么时候该沉淀、写到哪个目录、frontmatter 与四段正文的硬格式、怎么查重、坑怎么从 LESSONS 升级成技能。当你说"记下这个做法/沉淀成技能/总结经验/下次还要干同样的活"，或刚干完一件多步的活、刚修掉一个难缠的坑、被用户纠正两次以上时，读本技能。
---

# 把干成的活沉淀成技能（经验包）

记忆与技能**分工不同，缺一不可**：

- **记忆**（\`<workspace>/.codex-harness/memory/\`）记「**是什么**」：事实、约束、纪律、踩过的坑（\`lessons/<分类>.md\`，**用户纠错单独一类**）。
- **技能**（\`<cwd>/.codex/skills/\`）记「**怎么做**」：一条可复用的操作流程，含确切命令与判据。

只有记忆没有技能 ⇒ 坑会反复出现：\`lessons/\` 里躺着"现象 → 解法"一行，但**没有可执行步骤**，换一天你还会再踩一遍。

## 何时沉淀

1. 干完一件**多步**的活（≥3 步、以后还会再干）之后；
2. 修掉一个**难缠的坑**（尤其花了好几轮才定位的）之后；
3. 用户**纠正你两次以上**同一件事（说明你的默认做法在这个项目里不对）；
4. 你发现自己在这个项目里**反复手搓同一套流程**时。

**不要**为此停下主任务；也不要在什么都没干成时硬造技能。

## 写到哪

| 范围 | 落点 | 引擎可见性 |
|---|---|---|
| **项目专属（默认）** | \`<cwd>/.codex/skills/<名>/SKILL.md\` | \`skills/list\` 里 \`scope:"repo"\`，**随项目走、原生发现**（已实证） |
| 跨项目通用 | \`$CODEX_HOME/skills/<名>/SKILL.md\` | \`skills/list\` 里跨项目可见 |

- ⛔ **不要**写 \`<workspace>/.codex-harness/skills/\`：引擎**不认**这个目录（实测 \`skills/list\` 里不出现）。\`.codex-harness/\` 是应用自己的项目数据目录（记忆层在用）。
- ⛔ 项目信任策略只挡 config / hooks / exec 策略，**技能照样加载** —— 项目级技能不需要用户先点「信任此项目」。

## 格式（硬要求）

\`\`\`markdown
---
name: <小写连字符，与目录同名>
description: <一句话说清"什么场景该用它"，含触发关键词 —— 它决定未来的你何时会加载它>
---

# <这个技能解决什么>

## 何时用
- <触发场景 / 判据>

## 步骤
1. <确切命令或动作，不要写"配置一下"这种空话>

## 坑
- <真踩过的、必须绕开的>

## 判据
- <怎么算做完了：可验证的检查>
\`\`\`

- frontmatter 缺 \`description\` 的技能**永远不会进能力清单**（等于没写）；
- \`name\` 与目录名必须一致，否则同一个技能会出现两条；
- 只写**可复用的做法**：临时路径、当天状态、某次任务的中间结果都不进技能。

## 步骤

1. **查重**：先列两个技能目录（或看能力清单），已有同类 ⇒ **改它**，不要新建第二个。
2. **写**：按上面的格式建 \`<名>/SKILL.md\`，正文写你**实际跑通的**命令与顺序（不是设想）。
3. **验**：\`name\` 与目录同名、\`description\` 非空、四段齐全；必要时跑一次 \`skills/list\` 确认新技能在里面。
4. **报**：回复末尾加一行 \`🧠 已沉淀技能：<名>（<路径>）\`。

## 坑

- 只写"步骤"不写"判据" ⇒ 下次不知道自己做完没有，技能会退化成流水账。
- 把一次性细节写进去 ⇒ 技能越长越没人用；宁可再拆一个更专的技能。
- 坑只在 \`lessons/\` 记一行、不升级成技能 ⇒ 同一个坑还会再踩（见下）。

## 判据

- 新技能出现在 \`skills/list\`（或目录里读得到），且 \`description\` 能让没上下文的你判断"该不该用它"；
- 让一个**不知道本次任务背景**的人（或新的会话）按它单独执行一遍，能跑通。

## 坑怎么升级成技能

\`lessons/\` 里（以及 \`project/MEMORY.md\`）**同一现象出现第二次** ⇒ 把它升级成技能：把"现象 + 解法"改写成"**步骤 + 判据**"，落点按上表。升级后在那一行补一句"已升级为技能 <名>"，避免以后重复升级。
`;
