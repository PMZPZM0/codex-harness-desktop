/**
 * builtin-skills 的「memory-hygiene」部分（09-23 新增，非搬迁）。
 * 元技能：记忆整洁与清理规则（用户 09-23：「安装记忆清洁技能…明确…去重与清理策略，保证记忆长期可用且结构清晰」）。
 * ⛔ 正文必须是字面量（预检【86】按源文件文本做安全扫描）；正文里每个反引号都要写成 \` 转义。
 * 机械面（八层规则表 / 白名单动作 / 二次确认文案）的单一真相源是 \`electron/memory-hygiene.ts\`
 *   （CLEANUP_RULES / HYGIENE_ACTIONS / HYGIENE_ACTION_LABEL）+ \`memory-layers.ts\`（层表与水位）；
 *   本文件是**动作面**（什么时候该清、能清什么、绝不动什么）。预检【107】盯规则表齐全与二次确认。
 */
export const MEMORY_HYGIENE_SKILL = `---
name: memory-hygiene
description: 记忆整洁与清理规则手册：八层各在什么时候整理、允许什么动作、哪些内容永不自动清、留痕落在哪；以及三个可手动触发的清理动作（清空冷存档 / 清理过期碎片 / 整理纪律格式）各自的后果。当常驻记忆出现「[Harness 记忆水位 · ⚠️ 已达 90% 蒸馏线]」、或用户说"记忆太满了/清理一下记忆/记忆该整理了"时读它。
---

# 记忆整洁：能归档就不删，保护项永不自动清

**三条原则**（先记住，下面所有规则都是它们的展开）：

1. **能归档就不删** —— 被压缩掉的内容先移进 L6 冷存档（可回溯）；只有"冷存档本身"允许被用户清空；
2. **保护项永不自动清** —— L0 用户手写内容、L2 的**用户纠错**、pinned 碎片，自动流程一律不碰；
3. **清理要留痕** —— 淘汰写 \`pruned.jsonl\`、蒸馏把原文**移动**进 \`archive/\`；谁被清、何时清，事后查得到。

## 八层各自什么时候整理、能做什么

| 层 | 落点 | 什么时候 | 允许的动作 | **永不自动清** |
|---|---|---|---|---|
| **L0** 用户档案 | \`USER.md\`（userData，跨项目） | **只有用户主动改**；系统永不触发 | 仅允许用户手改 | 整层都是用户手写 ⇒ 全保护 |
| **L1** 项目宪法 | \`project/MEMORY.md\` | 水位 ≥90%，或用户点「蒸馏」 | 原地压缩：合并同主题、删已过时；**被删内容先移进 L6** | 用户手写段落 |
| **L2** 纪律与记忆 | \`lessons/<分类>.md\` | 水位 ≥90%，或同现象重复写入时 | 同现象只留一条（去重）；超 120 行提示整理；升级为技能后在行尾标注 | **「用户纠错」整类**（只许压缩措辞，不许删条） |
| **L3** 项目背景 | \`project/BACKGROUND.md\` | 水位 ≥90% | 合并同主题；稳定下来的约束**升格**进 L1（不是删） | 用户手写段落 |
| **L4** 每日日志 | \`logs/YYYY-MM-DD.md\` | 满 30 天，或水位 ≥90%（取最老一半） | 蒸馏成纪要进 L5、仍成立的事实进 L1；原文**移动**进 L6 | 无（但只移动、不删除） |
| **L5** 月度卷宗 | \`rollups/YYYY-MM.md\` | 水位 ≥90% | 压缩进 L1；月卷本身保留（回溯层） | 无（保留不删） |
| **L6** 冷存档 | \`archive/*.md\` | **只在用户手动清空**时 | 默认永久保留、不参与注入；可「清空冷存档」释放空间 | 无（清空是用户显式决定） |
| **L7** 碎片池 | \`memory.json\` | 每 6 小时自动检查；或用户点「清理过期碎片」 | 过期（临时上下文超 14 天）与超容量（500 条）按价值淘汰 | \`pinned\` 与分类「用户纠错」 |

## 三个可手动触发的清理动作（**都有后果，必须二次确认**）

| 动作 | 做什么 | 危险在哪 |
|---|---|---|
| **清空冷存档** \`purge-archive\` | 删除 L6 \`archive/\` | **删除后无法回溯** —— 被压缩掉的原文永久消失 |
| **清理过期碎片** \`prune-pool\` | 按规则淘汰 L7 碎片 | pinned 与「用户纠错」豁免；淘汰写 \`pruned.jsonl\` |
| **整理纪律格式** \`tidy-lessons\` | 只规范 \`lessons/*.md\` 的空白/空行 | **不改写任何内容**（纯格式） |

**你（引擎）不直接执行这三个动作** —— 它们走应用 IPC，且 **\`confirm !== true\` 会被直接拒绝**（守卫【107】）。
你的职责是**判断 + 建议**：把"该清什么、为什么、后果是什么"讲清楚，让用户决定。

## 你什么时候该动手（不需要用户吩咐）

看到常驻记忆里的水位提示就**先蒸那一层**（步骤见内置技能 \`memory-distill\`）—— 那是"整理"不是"清理"，不丢内容。

顺手整洁（每次写记忆时就做，别攒）：

1. 写之前**去重**（同现象已存在就不写；新结论修正旧行就改那一行）—— 见 \`memory-classify\`；
2. 一条一现象、带日期、带分类标记、不超 320 字；
3. 发现 \`lessons/\` 超 120 行，**在回复里提一句**建议整理（并说明整理会做什么），不要自作主张删。

## 判据

- 被蒸的层水位掉到 90% 以下（最好 <70%）；\`archive/\` 里能找到被消费掉的**每一份**原文；
- \`lessons/*.md\` 里没有重复现象、没有缺日期/缺标记的行（应用侧整洁页会把这些列成待办）；
- **一条纠错都没少**（\`corrections.md\` 条目数只增不减）；
- 涉及删除的动作**一律**先给出后果说明 + 等用户确认，绝不代替用户点。

## 坑

- **把"压缩"做成"删掉"**：纪律、纠错、用户偏好一律不许丢；拿不准就移进 \`archive/\`。
- **自动清理被误当成"优化"**：pinned 与纠错类豁免是**硬规则**，不是建议。
- **在日志层直接删旧日志**：必须**移动**到 \`archive/\`（回溯全靠它）。
- **自己实现一套清理规则**：别再写第二份规则表 —— 应用侧 \`memory-hygiene.ts\` 是单一真相源，
  你按它办事即可（写两套必然有一天两边口径不一致）。
- **不看后果就建议清空**：\`purge-archive\` 是**不可回溯**的，建议时必须把这句话原样带上。
`;
