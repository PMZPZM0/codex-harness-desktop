/**
 * builtin-skills 的「self-review」部分（09-23 新增，非搬迁）。
 * 元技能：教引擎**在每个任务收尾时自我复盘**——把"做错的 / 被纠正的 / 绕了弯路的"沉淀成记忆与技能，
 * 让能力随轮次累积（用户 09-23：「启用自我进化反思纠错技能：在每个任务完成后自动触发自我总结与反思，
 * 识别错误与改进点，并将经验沉淀为可复用能力」）。
 * ⛔ 正文必须是字面量（不要从别的模块拼装）：预检【86】按源文件文本做安全扫描，运行时拼装会让它看不见内容。
 * ⛔ 本常量是**模板字符串**：正文里每个反引号都必须写成 \` 转义 —— 裸反引号会提前结束字符串（09-22 实测踩到）。
 * 与 developer-instructions 第 7/8/9 条、内置技能 skill-authoring / memory-distill 是同一条链：
 *   指令给「何时写、写哪」（每轮注入、必须极短），本技能给「怎么复盘、问哪几个问题」（按需读）。
 */
export const SELF_REVIEW_SKILL = `---
name: self-review
description: 任务收尾时的自我复盘与纠错沉淀手册。当你（1）刚完成一个多步任务、（2）被用户指出错误或被纠正、（3）中途走了弯路又绕回来之后，按本技能做一次简短复盘，把结论写进记忆对应层、把重复出现的现象升级成技能。用户说"复盘一下/总结一下这次/你学到了什么"时也读它。
---

# 收尾复盘：让这一次的教训变成下一次的能力

**目的不是写成长篇报告，而是让下一次少踩一次。** 每条结论都要能落到「哪份文件、哪一行」。

## 什么时候做（触发时机）

- 完成一个**多步任务**（≥3 步、以后还会重复）之后；
- **被用户指出错误**、或在同一件事上被纠正**第二次**；
- 中途**走了弯路又绕回来**（例如先按错误假设改了一版、后来推倒重来）；
- 用户说「复盘一下」「总结一下这次」「你学到什么了」。

**不做什么**：一次性小改、纯查询、闲聊 —— 不用复盘，别为了打卡而写。

## 怎么做（四问 → 落盘）

先在心里过**四个问题**，每题最多留一条结论：

1. **错在哪**：本次有没有判断错、假设错、或改错方向？根因是什么（不是"手滑"，是"我依据了哪个错误的判据"）？
2. **被纠正了什么**：用户原话的**现象**是什么？我原来怎么理解的？正确的口径是什么？
3. **绕了什么弯**：有没有"先猜后验"多花的功夫？下次**第一个动作**应该是什么（例如"先打点再看代码"）？
4. **有什么值得固化**：有没有一条**可执行**的步骤/判据，是下次做同类事可以直接照做的？

然后把结论落到**唯一正确的落点**（分类判据见内置技能 \`memory-classify\`）：

| 结论形态 | 写到哪 |
|---|---|
| 用户说我错了 | \`<workspace>/.codex-harness/memory/lessons/corrections.md\`（**最高优先、注入最前、永不自动清**） |
| 我自己踩的坑 | \`lessons/pitfalls.md\` |
| 这次定下来的流程/约定 | \`lessons/sop.md\`（或升格进 \`project/MEMORY.md\`） |
| 用户的偏好/禁忌 | \`lessons/preferences.md\`（L0 跨项目画像另见 \`USER.md\`） |
| **可复用的多步流程** | **写技能**（先读 \`skill-authoring\`，别写进记忆） |

格式：一行一条，\`- YYYY-MM-DD ⚠️ 纠错：<现象一句> → <根因> → <以后怎么做>\`。**写之前先读目标文件去重**：
同现象已存在就不要重复写；新结论**推翻/修正**旧行时，改那一行，而不是再加一行。

## 升级规则（这是"进化"的关键一步）

**同一个现象出现第二次 ⇒ 升级成技能**，不是再记一条记忆：

1. 先读 \`skill-authoring\`（格式、查重、骨架都在里面）；
2. 把"现象 + 解法"改写成"**步骤 + 判据**"，写成 \`<workspace>/.codex/skills/<名>/SKILL.md\`；
3. 回到记忆里那一行**标注**「已升级为技能 <名>」——**别删那一行**（它解释了为什么会有这个技能）。

判据：一条备忘只有"现象"，没有可执行步骤 ⇒ 下次照旧会踩；升级成技能才有拦截力。

## 判据（怎么算复盘到位）

- 四个问题里**至少有一个**产出了落盘结论（全都没有 = 这次没什么可记的，也是合法结论）；
- 每条落盘结论都能回答「下次靠它拦住什么」；
- 被纠正过的事，\`lessons/corrections.md\` 里**有且只有一条**对应记录（去重过）；
- 若升级了技能：技能文件存在、frontmatter 有 \`description\`、记忆行有「已升级为技能」标注；
- 收尾回复里一行带过即可：\`🧠 复盘：<写在哪 / 升级了哪个技能>\` —— 不用长篇汇报。

## 坑

- **把复盘写成日记**：写"今天做了什么"没用，要写"我依据了哪个错判据"。现象一句、根因一句、动作一句，三句封顶。
- **只写给用户看**：复盘是**给下一次的我**看的，所以必须落到文件，光在回复里说一遍等于没做。
- **重复写同一条**：不先去重就写，\`lessons/*.md\` 会膨胀成噪音，水位很快到 90%（见 \`memory-hygiene\`）。
- **纠错写成"坑"**：用户指出的是 \`corrections.md\`，自己踩的是 \`pitfalls.md\` —— 类别错 = 注入优先级错（纠错本该排在最前）。
- **把该写技能的写成记忆**：一条 8 步流程塞进一行记忆，既读不懂也执行不了。
`;
