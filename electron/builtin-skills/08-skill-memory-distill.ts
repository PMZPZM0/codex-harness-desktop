/**
 * builtin-skills 的「memory-distill」部分（09-22 新增，非搬迁）。
 * 元技能：教引擎**自己蒸馏记忆金字塔**（用户 09-22：「每层满 90% 就往下蒸一层核心记忆，必须让 Codex 自己完成」、
 * 「记忆存储要分类，记忆应该单独一个目录，文件夹保存应该不会乱」）。
 * ⛔ 正文必须是字面量（不要从 memory-layers 拼装）：预检【86】按源文件文本做安全扫描，运行时拼装会让它看不见内容。
 * ⛔ 本常量是**模板字符串**：正文里出现的每个反引号都必须写成 \` 转义 —— 裸反引号会提前结束字符串（09-22 实测踩到）。
 * 与 electron/memory-layers.ts 的 MEMORY_PYRAMID / MEMORY_DISTILL_THRESHOLD / 目录常量是同一条链的两端：
 *   那边是**机械**（层表 / 水位 / 触发 / 目录布局），这边是**动作**（怎么压得不丢东西）。预检【104】【106】盯两边一致。
 */
export const MEMORY_DISTILL_SKILL = `---
name: memory-distill
description: 记忆金字塔的蒸馏操作手册：当注入的常驻记忆里出现「[Harness 记忆水位 · ⚠️ 已达 90% 蒸馏线]」时，按本技能把那一层压下去（logs→rollups+archive、rollups→project/MEMORY.md、project/MEMORY.md 原地合并、lessons 先升级技能再压）。也适用于用户说"记忆太满了/蒸馏一下记忆/该整理了"。
---

# 记忆金字塔：怎么蒸（不丢东西的那种）

工作区记忆是**漏斗**：越靠上越核心、越靠下越细碎。**全部落在一个目录** \`<workspace>/.codex-harness/memory/\`，
按层 / 分类分子目录（09-22 起：文件夹保存，不会乱）：

| 层 | 落点（相对 memory/） | 是什么 | 预算 | 满了沉到 |
|---|---|---|---|---|
| **L0** | \`USER.md\`（在应用 userData 下，跨项目） | 用户档案：偏好 / 禁忌 / 是谁 | 4000 | 人工精简（**不自动删**） |
| **L1** | \`project/MEMORY.md\` | 项目宪法：约定 / 选型与理由 / 结论 | 12000 | 同层压缩：合并同主题、删过时 |
| **L2** | \`lessons/corrections.md\`（**用户纠错**）· \`lessons/pitfalls.md\`（任务经验）· \`lessons/sop.md\`（工作流/SOP）· \`lessons/preferences.md\`（用户偏好） | 纪律与记忆：**一个分类一个文件** | 8000 | **先升级为技能**（skill-authoring）再压缩 |
| **L3** | \`project/BACKGROUND.md\` | 项目背景：快速入门 | 6000 | 稳定下来的升格进 L1 |
| **L4** | \`logs/YYYY-MM-DD.md\` | 每日日志（自动捕获） | 40000（每天 8000 × 回灌 5 天） | 沉 L5 月卷，原文进 L6 |
| **L5** | \`rollups/YYYY-MM.md\` | 月度卷宗（比日志短、比 L1 细） | 12000 | 沉 L1 |
| **L6** | \`archive/*.md\` | 冷存档（原文，可回溯） | — | 终态，**不进注入** |
| **L7** | \`memory.json\` | 碎片池（按 TTL 淘汰） | — | 淘汰记录进 \`pruned.jsonl\` |

⛔ **「用户纠错」是单独一类**（用户点名）：落 \`lessons/corrections.md\`，注入时排最前，且**永不参与自动淘汰**
—— 它一旦丢了就等着再犯同一个错。

**触发**：每层到 **90%** 就该蒸。触发不用你算 —— 注入的常驻记忆里会直接出现：

\`\`\`
[Harness 记忆水位 · ⚠️ 已达 90% 蒸馏线，先蒸馏再继续干活]
- L4 每日日志 已到 95%（38000/40000 字）⇒ 蒸馏去向：沉 L5 月度卷宗（原文进 L6 冷存档）
\`\`\`

看到这个提示就**先蒸那一层，再继续当前任务**（用户明确要求这件事由你做，而不是应用代劳）。应用侧也会在日志满 30 天或满水位时自动蒸一次；开始前看一眼 \`.distill-state.json\` 的 \`lastDistillAt\`，刚蒸过就别重复劳动。

## 怎么蒸（用你的文件工具直接改，全部保持短句）

1. **L4 每日日志 → L5 + L6**
   - 把要沉的那几天的日志提炼成**阶段纪要**，**追加**到 \`rollups/YYYY-MM.md\`（YYYY-MM 取那批日志里最新一天的月份），加一行 \`## <今天> 蒸馏（覆盖 <首日> ~ <末日>）\`；
   - 原文**移动**到 \`archive/<同名>.md\`（移动，不是删除 —— 蒸馏出错也能回溯）；
   - 其中**仍然成立**的事实/约束，升格进 \`project/MEMORY.md\`。
2. **L5 月卷 → L1**：把月卷压成"这个月定下来的事"，追加进 \`project/MEMORY.md\`，按主题合并（同一主题不要出现两节）。
3. **L1 项目宪法 → 原地重写**：合并同主题、删掉已过时的；删掉但可能还有用的内容移进 \`archive/\`，不要直接丢。
4. **L2 纪律与记忆 → 先升级、后压缩**：**逐分类文件**处理 \`lessons/*.md\`；重复出现的坑先按 \`skill-authoring\` 升级成技能，再压缩文件；
   \`lessons/corrections.md\`（用户纠错）**只许压缩措辞，不许删条**。
5. **L0 / L3**：合并同主题即可；**用户自己写的内容要删之前先问**。

## 坑

- 把"压缩"做成"删掉" —— 纪律、纠错、用户偏好一律不许丢；拿不准就移进 \`archive/\`。
- 在日志层直接删旧日志 —— 必须**移动**到 \`archive/\`（回溯全靠它）。
- 只压 L4 不看 L1 —— 压完 L4 的内容要有去处：纪要去 L5、仍成立的事实去 L1，否则等于白干。
- 把新文件写回旧布局（\`memory/MEMORY.md\`、\`memory/LESSONS.md\` 这种）—— v2 起一切都在子目录里，写错位置 = 下一轮读不到。
- 蒸完不留痕 —— 应用侧靠 \`.distill-state.json\` 节流，你手动蒸过也把 \`lastDistillAt\` 更新成当前毫秒时间戳。

## 判据

- 被蒸的那层水位**掉到 90% 以下**（最好 <70%）；
- \`archive/\` 里能找到被消费掉的**每一份**原文；
- 蒸馏后的文本里，原来那些「现象 → 根因」的纪律与纠错**一条不少**；
- 回复里用一行说明：「🧠 已蒸馏 L4 → L5/L6（覆盖 X~Y）」。
`;
