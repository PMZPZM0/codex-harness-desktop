/**
 * builtin-skills 的「memory-classify」部分（09-23 新增，非搬迁）。
 * 元技能：写记忆**之前**的分类口径（一条一现象、四分类、优先级、去重）。
 * 用户 09-23：「安装记忆分类技能…明确记忆的写入、分类归档、去重与清理策略」。
 * ⛔ 正文必须是字面量（预检【86】按源文件文本做安全扫描）；正文里每个反引号都要写成 \` 转义。
 * 机械面（分类名 / 优先级 / 落点）的单一真相源是 \`electron/memory-lessons.ts\`
 *   （CORRECTION_CATEGORY / PITFALL_CATEGORY / SOP_CATEGORY / PREFERENCE_CATEGORY / MEMORY_CATEGORY_ORDER）；
 *   本文件是**动作面**（怎么判断该写哪一类）。两处口径必须一致 —— 预检【105】盯分类判定。
 */
export const MEMORY_CLASSIFY_SKILL = `---
name: memory-classify
description: 写记忆前的分类与去重口径。凡是要往工作区记忆里写一条东西（踩的坑、被纠正的事、定下来的流程、用户的偏好），先读本技能判断该进哪个文件、用哪一行格式、跟谁去重。用户说"记一下这个/记到记忆里/分类整理记忆"时也读它。
---

# 记忆分类：一条一现象，进对文件

记忆不是日记。**每条只讲一件事**，并且必须进到**唯一正确的那一类** —— 分错类的代价不是"不整齐"，
而是**注入顺序错**（「用户纠错」要排最前、且永不自动淘汰，混进别的类就享受不到）。

## 四个分类（按判定优先级：纠错 > 任务经验 > 工作流/SOP > 用户偏好）

| 分类（文件） | 什么算这一类 | 判据（问自己） |
|---|---|---|
| **用户纠错** \`lessons/corrections.md\` | 用户明确指出我错了 / 给了正确口径 | 「这是不是**用户说的**我错了？」→ 是就进这里，**不问其他** |
| **任务经验** \`lessons/pitfalls.md\` | 我自己踩的坑、修掉的回归、复现过的怪现象 | 「没人纠正我，是我自己撞上的？」 |
| **工作流/SOP** \`lessons/sop.md\` | 本项目定下来的流程、约定、命令口径 | 「这是"该怎么做"的规矩，不是"我错了"？」 |
| **用户偏好** \`lessons/preferences.md\` | 用户的偏好 / 习惯 / 禁忌 | 「这是关于用户本人怎么想的？」 |

分类名与落点**一个分类一个文件**（不要全塞进一个 \`LESSONS.md\`，那是 v1 的旧布局，已迁移走）。
跨项目的**用户画像**（角色、技术栈偏好、长期禁忌）不属于工作区记忆 ⇒ 进 L0 \`USER.md\`。

## 一条长什么样

\`\`\`
- 2026-09-23 ⚠️ 纠错：把「内容变矮」当成用户滚动 → 跟随被关掉 → 折叠后留白不再自愈 → 贴合要覆盖钉顶路径
- 2026-09-23 约定：本仓库临时产物一律写 .workbuddy/tmp/，禁止落仓库根与盘根
- 2026-09-23 偏好：用户要求「改完直接提交，我来验收」
\`\`\`

- **一行一条**，三段式：\`现象（一句）→ 根因 → 以后怎么做\`；
- **必须带日期** \`YYYY-MM-DD\`（没有日期，蒸馏时无法判断新旧 ⇒ 预检【107】会报 \`lesson-no-date\`）；
- **必须带分类标记**（\`⚠️ 纠错：\` / \`⚠️ 坑：\` / \`约定：\` / \`偏好：\`），缺标记会报 \`lesson-no-mark\`；
- 单条**别超 320 字**（超了说明你在写叙事，不是写纪律；该拆成多条或升级成技能）。

## 去重规则（写之前必做）

1. **先读目标文件**，不要凭印象；
2. 同现象已存在 ⇒ **不写**（已有的那条赢）；
3. 新结论**修正/推翻**旧行 ⇒ **改那一行**，不要新增（两行并存会让下一个人不知道该听谁的）；
4. 同一现象出现**第二次** ⇒ 走 \`self-review\` 的升级规则，写成技能，并在记忆行标注「已升级为技能 …」。

## 不该写进记忆的

- **可复用的多步流程** ⇒ 写技能（\`skill-authoring\`），记忆只放一行指路；
- **每日做了什么** ⇒ 应用已自动捕获到 \`logs/YYYY-MM-DD.md\`，不要重复记；
- **临时路径 / 一次性的中间结果 / 工具报错原文** ⇒ 不记（没有跨会话价值）；
- **用户手写的段落** ⇒ 不要动、不要重排、不要改写（那是用户的财产）。

## 坑

- **一条塞三件事**：去重失效、蒸馏时无法归类。
- **把"用户偏好"当"用户纠错"**：偏好进 \`preferences.md\`；纠错是"我错了"，别把每条喜好都升级成最高优先。
- **写回 v1 旧文件**（\`memory/MEMORY.md\`、\`memory/LESSONS.md\`）：那是迁移前的布局，写了下一轮读不到。
- **L1 \`project/MEMORY.md\` 不要手写**：它是蒸馏产物，手写会被下一次蒸馏冲掉；要沉淀就先落到 \`lessons/\` 或 \`project/BACKGROUND.md\`。
`;
